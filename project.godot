; Engine configuration file.
; It's best edited using the editor UI and not directly,
; but the file format is supported, so it's possible.

; [section] ; comment
; key=value

config_version=5

[application]

config/name="2D Fighting Game"
config/features=PackedStringArray("4.3", "Forward Plus")
config/tags=PackedStringArray("multiplayer", "fighting", "2d")

[rendering]

renderer/rendering_method="forward_plus"
